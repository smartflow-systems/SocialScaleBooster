run = "pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT --reload"

[env]
PYTHONUNBUFFERED = "1"
REPLIT_WEBVIEW = "demo.html"           # Auto-opens your landing page

[deployment]
run = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[nix]
channel = "stable-25_05"