run = "pip install --user -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT --reload"

[env]
PYTHONUNBUFFERED = "1"
REPLIT_WEBVIEW = "demo.html"           # Opens your landing page automatically

[deployment]
run = "uvicorn main:app --host 0.0.0.0 --port $PORT"